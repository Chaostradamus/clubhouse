<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= isAdmin ? 'Become Admin' : 'Join Club' %> - Members Only</title>
    <style>
        body { font-family: Arial, sans-serif; max-width: 500px; margin: 0 auto; padding: 20px; }
        .error { color: red; }
        form { display: flex; flex-direction: column; }
        input, button { margin-bottom: 10px; padding: 8px; }
    </style>
</head>
<body>
    <h2><%= isAdmin ? 'Become an Admin' : 'Join the Club' %></h2>

    <% if (error) { %>
        <p class="error"><%= error %></p>
    <% } %>

    <p>Enter the secret passcode to <%= isAdmin ? 'become an admin' : 'join the club' %> and see who wrote each message!</p>

    <form action="<%= isAdmin ? '/become-admin' : '/join-club' %>" method="POST">
        <input type="password" name="passcode" placeholder="Secret Passcode" required>
        <button type="submit"><%= isAdmin ? 'Become Admin' : 'Join Club' %></button>
    </form>
</body>
</html>